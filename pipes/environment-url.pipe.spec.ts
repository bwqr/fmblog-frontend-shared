import { EnvironmentUrlPipe } from './environment-url.pipe';

describe('EnvironmentUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new EnvironmentUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
